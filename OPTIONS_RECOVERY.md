# TTPlayer 选项窗口恢复证据与行为基线

本文记录从 `reverse/decompiled/TTPlayer.exe.pseudo.c` 静态恢复，并由历史
Windows Sandbox 证据及当前宿主机双版本对照确认的选项窗口行为。地址均为
原版 `TTPlayer.exe` 的虚拟
地址；伪代码行号会随重新导出而变化，因此实现和评审时应优先使用函数地址。

## 1. 总体结构

原版不是十五个互不相关的对话框，而是一个模式无关属性表外壳，加十五个固定
顺序的页面。核心调用链如下：

- `FUN_0045D531`：销毁已有选项对象，记录目标页，构造新的选项对象并显示；
- `FUN_0049F4AD`：构造完整页面集合并按固定顺序加入属性表；
- `FUN_00468BE7`：创建/显示模式无关属性表；
- `FUN_0049F9BE`：把标准属性表改造成原版外壳，创建左侧导航、页标题、
  “全部保存”“全部重置”和相关链接；
- `FUN_004345B4`：切换当前属性页；
- `FUN_0049FD89`：定位普通控件，或切换歌词/网络页内部的子页后再聚焦控件；
- `FUN_0049FE39`：处理全部保存、全部重置、关闭和属性表生命周期。

`FUN_0049F4AD` 在伪代码约 150982--151048。它先构造系统关联对象
`FUN_00491213(puVar1 + 0x32f)`，再构造全屏对象 `FUN_00491516`；但是
`FUN_004A15BD` 的实际加入顺序是全屏在前、系统关联在后。因此不能按构造顺序
推断最后两页。

## 2. 十五页的精确顺序

页索引是从零开始的内部索引，也是定向入口使用的索引。

| 索引 | 页面 | 对话框资源 | 页面消息过程 | 初始化/填充函数 | 主要持久化域 |
|---:|---|---:|---|---|---|
| 0 | 关于 | 200 | `FUN_0048EC9F` | `FUN_00492337` | 无 |
| 1 | 常规 | 250 | `FUN_0048ED40` | `FUN_0049257C` | `General`、部分 `Player` |
| 2 | 播放 | 251 | `FUN_0048EFD8` | `FUN_00492C57` | `Playback` |
| 3 | 快捷键 | 252 | `FUN_0048F1BB` | `FUN_004932A1` | `HotKey` |
| 4 | 视觉效果 | 253 | `FUN_0048F28A` | `FUN_004937F4` | `Visual` |
| 5 | 播放列表 | 254 | `FUN_0048F493` | `FUN_00493FC9` | `PlayList` |
| 6 | 媒体库 | 255 | `FUN_0048F799` | `FUN_004948E1` | `Library` |
| 7 | 歌词秀 | 256 | `FUN_00490276` | `FUN_004974F0` | `Lyric`、`DeskLrc` |
| 8 | 歌词搜索 | 257 | `FUN_004905E6` | `FUN_00497B64` | `Lyric` 的搜索/下载字段 |
| 9 | 网络连接 | 258 | `FUN_00490AB9` | `FUN_0049885A` | `Network` |
| 10 | 音效插件 | 259 | `FUN_00490CB8` | `FUN_00498E25` | `Plugin` |
| 11 | 音频设备 | 260 | `FUN_00490E80` | `FUN_00499498` | `Device` |
| 12 | 皮肤 | 261 | `FUN_00491093` | `FUN_0049AB1E` | `Skin/@PackageName` |
| 13 | 全屏显示 | 263 | `FUN_00491571` | `FUN_0049ECD0` | `FullScreen` 及全屏歌词字段 |
| 14 | 系统关联 | 262 | `FUN_0049135B` | `FUN_0049D2FA` | 注册表；检查开关在 `Player` |

歌词页 256 内含两个子对话框：普通歌词 384 和桌面歌词 385。网络页 258 内含
两个子对话框：网络缓存 381 和下载设置 382。`FUN_0049FD89` 对活动页索引 7
调用 `FUN_0049744B`，对索引 9 调用 `FUN_00498774`，其余页面才直接
`GetDlgItem` 并发送 `WM_NEXTDLGCTL`。

## 3. 所有已确认的进入路径

### 3.1 主命令入口

主窗口 `FUN_004582D0` 的 `WM_COMMAND` 分派位于伪代码约
88660--88950。已确认的命令和目标页如下：

| 命令 | 十进制 | 目标索引 | 页面 | 入口语义 |
|---:|---:|---:|---|---|
| `0xE140` | 57664 | 记忆页或 `lParam` 指定页 | 通用选项 | 先退出全屏，再打开完整选项 |
| `0x8084` | 32900 | 4 | 视觉效果 | 直接定向打开，不先退出全屏 |
| `0x7EFD` | 32509 | 5 | 播放列表 | 直接定向打开，不先退出全屏 |
| `0x7FEF` | 32751 | 6 | 媒体库 | 直接定向打开，不先退出全屏 |
| `0x8023` | 32803 | 7 | 歌词秀 | 直接定向打开，不先退出全屏 |
| `0x7918` | 31000 | 12 | 皮肤 | 直接定向打开，不先退出全屏 |

`FUN_004658A3`（约 98791）是通用 `0xE140` 的包装：先调用
`CPlayerWnd_ExitFullscreenBeforeMenu`，再调用 `FUN_0045D531`。这是这些入口中
唯一固定先退全屏的路径。定向命令在 `FUN_004582D0` 中直接走
`FUN_0045D531`，不应为了代码复用而统一调用退全屏逻辑。

通用入口的 `lParam` 有私有页索引语义：值在 1..15 时传给选项构造；普通菜单
传 0 时使用 `Histroy/@LastActivePage`。属性表实际只有 15 页，所以索引 15
最终不能命中页面并回落到第 0 页。实现不可把 15 强行夹成第 14 页。

原版默认快捷键表的第一项是 `F1 -> 0xE140`，所以 F1 与普通“千千选项...”走
同一入口并恢复记忆页。主窗口右键菜单、托盘菜单和皮肤菜单中的普通选项项也
应汇入这一命令，而不是各自创建另一份属性表。

### 3.2 定位到子页或控件的入口

主窗口私有消息 `0x7F4` 的语义为：

- `wParam`：要聚焦的控件 ID，或歌词/网络内部子对话框资源 ID；
- `lParam`：外层的零基页索引；
- 主窗口在 `FUN_00464900` 中打开目标页，再调用 `FUN_0049FD89` 完成定位。

已确认实例：

- 桌面歌词菜单命令 `0x8046` -> `FUN_00419372` ->
  `PostMessage(main, 0x7F4, 0x181, 7)`；`0x181` 即子模板 385；
- `FUN_004251C0` -> `PostMessage(main, 0x7F4, 0x17E, 9)`；
  `0x17E` 即子模板 382；
- 独立歌词搜索对话框的“相关设置”控件 `0x889` -> `FUN_0043C28E` ->
  `PostMessage(main, WM_COMMAND, 0xE140, 9)`，定向到网络连接页；
- 选项窗口“歌词搜索”页内的同 ID 控件则走 `FUN_00497F63`：直接向
  当前属性表发送 `PSM_SETCURSEL(9)`，保持同一个属性表 HWND。

跨窗口入口使用 `PostMessage` 是生命周期要求；已经位于选项属性表内部时则应
原地切页。把后者错误地改为重新打开选项，会从当前子页回调中同步销毁属性表，
从而重入 `comctl32`，造成未响应或访问已销毁页面。

## 4. 外壳、关闭、Apply 与持久化语义

原版底部可见按钮是：

- `0x4D2`：全部保存（资源 `0x8141`）；
- `0x4D3`：全部重置（资源 `0x8140`）；
- `IDOK`：文本改为“关闭”；
- 标准 Cancel 隐藏，标准 Apply 不是正常可见工作流。

`FUN_0049FE39`（约 151505--151559）的精确行为：

1. `0x4D2` 调用 `FUN_0040374B`，把当前内存设置立即写入配置。
2. `0x4D3` 先用资源 `0x8143` 询问。确认后保存当前页索引，销毁窗口，调用
   `FUN_004282E4`、`FUN_0043A26B`、`FUN_00401E96` 重建默认配置和相关对象，
   然后依次投递 `main, 0x7F0, 0xFFFF, -2` 与
   `main, WM_COMMAND, 0xE140, 0` 重新打开。
3. `IDOK` 与 `IDCANCEL` 完全走同一关闭分支：投递
   `main, 0x7F0, 0x100, -1`，记录当前页到 `DAT_00547E70`，再销毁属性表。
   Cancel 没有事务回滚语义。
4. 页面控件通常在点击、选择变化或编辑提交时就写入进程内设置并通知对应
   子系统；关闭属性表本身通常不立即改写 `TTPlayer.xml`。应用退出时统一保存，
   “全部保存”才是窗口内显式立即落盘入口。

最后活动页必须按原版拼写保存为 `Histroy/@LastActivePage`，不能改成
`History`，否则旧配置不能往返。

## 5. 页面运行时通知边界

原版通过主窗口私有消息 `0x7F0` 的位掩码按子系统即时应用。下列映射有直接
伪代码证据：

| 功能域 | 掩码 | 证据示例 |
|---|---:|---|
| 视觉效果 | `0x0001` | `FUN_004939E9` 等 |
| 常规 | `0x0002` | `FUN_004927D5` 附近的页面处理器 |
| 播放 | `0x0004` | `FUN_00492E..` 页面处理器 |
| 快捷键 | `0x0008` | 快捷键页离开时发送 `(8, -1)` |
| 普通歌词 | `0x0010` | `FUN_00496E12` 等 |
| 播放列表 | `0x0040` | `FUN_0049413B` 等 |
| 媒体库 | `0x0080` | `FUN_00494750`、`FUN_0049498D` 等 |
| 网络/下载 | `0x0100` | `FUN_00498705`；整个属性表关闭也发送 `(0x100, -1)` |
| DSP 插件 | `0x0200` | `FUN_00498E25`、`FUN_00498F7B` |
| 音频设备 | `0x0400` | `FUN_0049989D`、`FUN_0049A08A`..`0049A33E` |
| 皮肤 | `0x0800` | `FUN_0049AEA3` |
| 桌面歌词 | `0x4000` | `FUN_00495DEC`、`FUN_004963C3` 等 |

通知的 `lParam` 常保留触发控件 ID，用于局部刷新。实现若每次控件变化都执行
“重载所有设置/重启所有子系统”，既不等价，也容易造成 UI 长时间未响应。

## 6. 关键页面的不可省略行为

### 6.1 快捷键页 252

- ListView `1064`，列资源 `0x815A`，宽度为 90/145/145；原版有 54 条记录。
- 全局快捷键总开关 `2053`；应用内 HOTKEY 控件 `2054`；全局 HOTKEY 控件
  `2055`；“设置”按钮 `1098`。
- 选中行时仅按“是否有选择”启用三个编辑控件；关闭全局总开关并不禁用
  `2055`。
- 点击设置时分别用 `HKM_GETHOTKEY` 读取两个值并更新当前 12 字节记录；原版
  在这里不做冲突对话框。
- 页面激活时注销已注册的全局热键，离开时通过 `0x7F0/0x8/-1` 重建注册，
  防止 HOTKEY 控件与播放器自己的全局注册互相抢占。
- 菜单资源标签应取第一个换行符之后的命令文本；命令 32000 还要与 32001
  的文本以 `/` 合并。

### 6.2 DSP 页 259

- 控件：目录编辑框 `1028`、浏览 `1023`、列表 `1064`、配置 `1015`、上移
  `1042`、下移 `1045`。
- 列资源 `0x815B` 的文本顺序为“描述|文件名”；实际列顺序是空白勾选列、
  描述、文件名，宽度 20/256/100。
- 同时扫描配置目录与 `exe\Plugins`（两者不同时），只接受 `dsp_*.dll`。
- 候选 DLL 必须可加载、导出 `winampDSPGetHeader2`，且头版本大于 `0x1F`；
  描述来自 DSP header，不能直接用文件 stem 伪造。
- 勾选/取消立即加载或卸载；配置按钮和双击调用已激活模块的配置回调；上移、
  下移和自定义通知 `-197` 修改实际 processor 顺序。
- `Plugin/@Folder` 保存插件目录；`Modules_Count` 和 `Modules_N` 保存活动模块。
  `Modules_N` 是完整 DLL 路径。界面第三列仅裁出文件名，不代表持久化只存
  文件名。
- 实时链已按 `FUN_00427EDC`、`FUN_00428061`、`FUN_004280CD` 和
  `FUN_0042898D` 接通：校验 header 版本、拒绝 PaceMaker、在模块槽 0..9
  中取第一个有效模块，依次调用 `Init/ModifySamples/Quit`。ABI 是不含
  `userData` 的旧 Winamp 结构；Win32 偏移分别为 `+0x10/+0x14/+0x18`。
- 原版在送入 DSP 前把 PCM 转成有符号 16 位，并在帧数大于 `0x3FFF` 时只拆成
  前后两段；`ModifySamples` 的返回帧数不参与调整。重建链保留这一行为，同时
  用 SEH/C++ 异常边界把出错模块从当前链摘除。DLL 内部永久阻塞仍不能在
  in-process 实时 ABI 中安全强杀；文件发现和配置窗口继续由限时辅助进程隔离。

### 6.3 音频设备页 260

- 输出组合框 `0x423` 枚举 Wave Mapper/全部 waveOut、DirectSound、
  Kernel Streaming、ASIO；选项携带原版 16 字节设备键。
- waveOut 顺序为 Wave Mapper 后接 `waveOutGetNumDevs` 的所有实体设备；
  DirectSound 使用 `DirectSoundEnumerateW`。KS 不能用设备实例 ID 的简单交集
  代替：原版先枚举 `KSCATEGORY_AUDIO`，再用
  `SetupDiGetDeviceInterfaceAlias(KSCATEGORY_RENDER)` 验证 render alias。
- KS 打开方式与 `FUN_004F28A8` 一致：`GENERIC_READ | GENERIC_WRITE`、不共享、
  `OPEN_EXISTING`、`FILE_ATTRIBUTE_NORMAL | FILE_FLAG_OVERLAPPED`，失败后不降级
  到共享或只读句柄。只有 communication=`KSPIN_COMMUNICATION_SINK`、
  dataflow=`KSPIN_DATAFLOW_IN`，并同时具有 standard-streaming interface 0 和
  standard-devio medium 0 的 PCM/WAVEFORMATEX pin 才作为输出设备发布。
- KS 属性探测继续使用 overlapped I/O；重建版给单次属性设置 1500 ms、给
  SetupAPI alias 枚举之后的 KS capability walk 设置 3000 ms 有界预算。取消后，
  请求/输出缓冲、事件和 `OVERLAPPED`
  由完成收口持有，不能在驱动仍可能完成 I/O 时释放栈内存。这防止选项页无限
  卡住，同时不放宽原版的设备接纳条件。当前宿主机原版和重建版的设备组合框
  均为 8 项、选中索引均为 4。
- `SetupDi*`、同步 `CreateFileW`、waveOut/DirectSound 驱动回调本身没有可靠的
  线程内取消边界。因此四类目录与当前已恢复的能力字段由同位数隔离工作进程
  一次生成版本化快照；2026-09-11 起改用主 EXE 自身的
  `--ttplayer-output-device-worker` 模式，不再依赖旁侧辅助 EXE。父进程用 kill-on-close
  Job 管理它，整个 helper 最多等待 4000 ms，终止后再给 1000 ms 有界收口。
  页面随后只消费快照，切换设备时不再重新进入驱动。该隔离是防未响应的安全
  边界，不改变成功探测时的原版顺序和设备键。
- worker 启动失败、超时、异常退出或快照损坏时，页面发布一个携带当前
  `Device/@DeviceType` 的不可推断行；关闭/Apply 因而不会把原设备静默改成
  Wave Mapper。当前实现仍在页面首次创建时同步等待 helper：正常等待期限为
  4000 ms，故障终止后还可占用最多 1000 ms 收口，因此故障路径约为 4+1 秒，
  并非后台异步刷新。
- 原版把 ASIO 注册发现与能力探测分开：发现阶段只接受
  `HKLM\SOFTWARE\ASIO` 下具有有效 `{CLSID}`、且对应
  `HKCR\CLSID\{...}\InprocServer32` 默认字符串可读的子键，不加载驱动；选择
  设备后才懒探测四行详情，即输出通道数 `%d Channels`、13 档
  `canSampleRate == 0` 的采样率列表加 `Hz`、首个输出通道有效位 `%d Bits` 和
  preferred buffer `%d Samples`。当前同位数 helper 已恢复 `004E207B` 的只读
  `init -> getChannels -> getBufferSize -> getChannelInfo(output 0) ->
  004E14BD -> 13*canSampleRate` 调用链和四行精确格式；它不调用
  `setSampleRate/createBuffers/start`。为避免 UI 线程进入可能挂死的旧驱动，helper
  在发布快照前物化这些原本懒计算的字段，这一点是有意的隔离时序差异。选定 CLSID
  的 ASIO 播放后端已恢复，并且失败时不会转到 waveOut。
- waveOut `dwSupport == 0` 时三个支持详情行保持空白，避免把“无能力位”错误显示
  成三个“否”；最终宿主机矩阵没有直接捕获这三行文本，因此这是实现审计结论，
  不是该报告单独证明的结果。
- 设备键的后端标记：GUID 尾部全零且首 DWORD 高字 0 为 waveOut，高字 2 为
  Kernel Streaming，高字 3 为 ASIO；DirectSound 使用其非零设备 GUID，不能
  仅凭首 DWORD 高字归入 waveOut。不要互换 KS 与 ASIO。
- 选择变化进入 `FUN_0049989D`：比较并复制设备键，重建能力列表，然后发送
  `main, 0x7F0, 0x400, 0x423`。
- 原始输出位数组为“原始比特”、8、16、24、32，实际存储值为选择索引乘 8，
  因而 0 是合法值。
- 重采样率精确集合：8000、11025、16000、22050、24000、32000、44100、
  48000、64000、88200、96000、176400、192000。
- Dither 取值 0=关闭，1..4=四种模式；不是布尔字段。
- 详情行必须按后端使用资源 `0x812A`（waveOut）、`0x812B`
  （DirectSound）、`0x812C`（KS）、`0x812D`（ASIO）构建。DirectSound 才
  显示硬件缓冲和主缓冲相关控件。
- buffer、输出位、dither、重采样开关/频率/质量、硬件缓冲、主缓冲的各个
  处理器 `FUN_0049A08A`..`FUN_0049A33E` 都即时发送 `0x400` 通知。

### 6.4 皮肤页 261

- 列表 `1064` 选择变化 -> `FUN_0049AC87`；双击 -> `FUN_0049AE8B` ->
  模拟点击应用按钮 `1098`；删除按钮 `1039`；下载链接 `2109`。
- 默认项显示资源 `0x81A6`（`<默认皮肤>`），但详细元数据来自内置
  `Skin.xml`：Author=`TTplayer`、URL=`http://www.ttplayer.com`、
  Email=`none`。不能用应用标题资源替换 Author。
- 外部项显示包内元数据，控件 `2001` 显示当前选择项的包/文件名，而不是已
  应用皮肤的全局名称。
- 应用通过 `main, 0x7F0, 0x800, package-path`；删除使用资源 `0x81A7`
  确认并删除对应包。下载链接为
  `http://ttplayer.qianqian.com/skin.htm`。
- 预览不是只拉伸背景图。`FUN_0049A6EF` 创建临时皮肤主窗口、绘制父窗口，
  枚举可见子窗口并向它们发送 `WM_PRINTCLIENT(0x317, PRF_CLIENT)` 后再缩放。
  已补回初始资源文字、固定 80% 音量、滑条填充、图标居中和 minimode 禁用帧；
  伪代码丢失的音量寄存器参数以 `00467F9B` 汇编校正，详见 [SKIN_PREVIEW.md](SKIN_PREVIEW.md)。
- `FUN_0049A372` 为默认项和每个外部包各持有独立的皮肤管理对象；预览不能
  借用正在显示的主窗口皮肤位图。`FUN_0049A6EF` 在合成后调用
  `FUN_00445709`，以 `GetSysColor(COLOR_WINDOW)` 替换包透明色，再由
  `FUN_0049A9A3` 做普通 HALFTONE 缩放；比例只允许缩小到预览框，不放大小
  皮肤。重建版按相同生命周期独立解包、加载和缓存预览，并以 `GetDIBits`
  读取背景、`BitBlt` 作为旧式 DDB 备用路径。
- 预览控件被子类化：`WM_PAINT/WM_PRINTCLIENT` 走 `FUN_0049B010`；
  `WM_RBUTTONUP` 走 `FUN_0049B21A` 导出 BMP。默认文件名是
  `Skin\Default_Skin.bmp`，外部 `.skn`/`.zip` 则替换扩展名为 `.bmp`。
- 重建版仍在右键根菜单打开时预取完整皮肤目录，但展开“皮肤”只发布已经完成
  的一代结果，不再在 UI 线程等待扫描。预览/应用皮肤包时直接调用 EXE 同目录
  已加载的 `ttpcomm.dll` deflate ABI 将条目物化到指纹缓存，不再进入可能长时间
  阻塞的 `Shell.Application/CopyHere` 路径；这也更接近原版内存 ZIP reader 的
  调用边界。

### 6.5 全屏页 263

- 主要控件：视觉类型 `2232`，统一设置 `2233`，并列/重叠 `2230`，歌词占屏
  `2231`，滚动 `1033`，对齐 `1037`，淡化距离 `1026`，行间距
  `1043/1044`，淡化当前行 `2145`，卡拉 OK `2150`，背景透明 `2151`，
  自动字体 `2022`，普通/高亮/背景色 `1155/1156/1158`，字体 `1036`。
- `FullScreen/@VisualType` 合法范围是 0..3，不能按布尔值夹成 0..1。
- 运行时语义为 `VisualType == 0` 使用 All profile；非 0 时按当前
  `Visual/@Type` 选择 Goom/Spectrum/BlurScope profile。数值 1/2/3 同时表示
  设置页当前编辑的 profile。
- 四套成对属性必须独立往返：
  `PosRelationAll/LrcSizeAll`、`PosRelationGoom/LrcSizeGoom`、
  `PosRelationSpectrum/LrcSizeSpectrum`、
  `PosRelationBlurScope/LrcSizeBlurScope`。
- 原版页初始化在非统一模式下默认回到编辑 Goom：`FUN_0049F14C` 会令
  `VisualType=1` 并把 `2232` 选到第 0 项；保留上次编辑的 2/3 项并不等价。
- 行间距 edit/spin `1043/1044` 的原版范围是 0..100，证据在
  `FUN_0049ECD0`/`0049EFE8`；不应夹成 0..50。
- 歌词淡化距离组合框先插入资源 `0x813E` 的“不使用”（item data 0），再按
  资源 `0x813F` 插入分母 3、4、5、6、8、10、12、16。显示索引不能直接当
  保存值。
- 若运行环境没有 `SetLayeredWindowAttributes`，原版从 `2230` 删除“重叠”，
  禁用 `2151`，并把受影响 profile 的关系默认成并列。现代 Windows 通常具备
  此能力，但兼容门仍应保留。
- 原版字体对话框 flags 为 `0x01000041`，即在常规 screen/init flags 上包含
  `CF_NOVERTFONTS`；选择成功后强制 `LOGFONT.lfQuality=4`。字体按钮还绘制
  16x16 位图资源 `0x161`。
- 三个颜色按钮先打开锚定在按钮下方的预置色弹层；自定义通知再进入完整
  `ChooseColor`。直接在第一次点击就打开系统颜色对话框能改变数值，但不是
  原版交互。

### 6.6 系统关联页 262

- TreeView `2038` 不是只遍历已加载的 AddIn。`FUN_0049CC0E` 先把 CD、CUE、
  MP3、MIDI、Wave、AIFF、AU 七个内置 reader 与已成功加载 DLL 的格式按完整
  描述排序，再把播放列表格式追加到末尾；随后 `FUN_0049D2FA` 从这份完整
  Open File filter 构造扩展名树。
- 每个格式项各自生成一个分类，不合并同名分类，也不跨 reader 去重扩展名；
  扩展名保留 filter 中的原始大小写。普通分类和叶节点使用 `TVI_SORT`，最后
  的播放列表分类使用 `TVI_LAST`。父、分组、叶节点使用三态勾选，点击父节点
  递归设置所有子项。
- 已确认的排除扩展名是 `avi`、`asf`、`wmv`、`rm`、`ram`、`rmvb`、
  `7z`、`zip`、`rar`；词法器从 pattern 中的字面 `.` 后开始取扩展名，黑名单
  比较忽略大小写。
- `WM_DESTROY` -> `FUN_0049D8C0` 才比较期望/原状态并批量写入扩展名关联，
  最后仅在发生变化时调用 `SHChangeNotify(SHCNE_ASSOCCHANGED, ...)`。因此
  OK、Cancel、关闭按钮都会提交扩展名关联。
- Audio CD `1069` 和文件夹 `2120` 是即时注册/取消；关联检查 `2121` 写入
  Player 设置。
- `2030/2031/2032` 分别创建桌面、Programs、Quick Launch 快捷方式；原版是
  动作按钮，不是待关闭时提交的复选框。
- 图标按钮的精确含义是：`2108`“选择文件图标”、`2216`“从文件夹自动选择
  图标”、`2106`“选择播放器图标”。`2106` 不是清空当前叶节点图标；原版
  `FUN_0049DDD9` 会弹出菜单资源 154，以命令 33000/33001 切换内置播放器
  图标或选择图标文件，并通知常规/播放器外观刷新。
- `2281`“手动设置关联”进入 `FUN_0049DFB4`：检查并启动 `ttpsvr.exe`，构造
  临时 `FileTypeAsso` XML，通过 Vista/Win7 提升辅助路径写关联。若重建版以
  现代按用户注册后端取代辅助程序，也必须给这个可见按钮等价动作或明确错误，
  不能留下无响应的死控件。
- 原版播放命令为 `"TTPlayer.exe" "%1"`，加入列表命令为
  `"TTPlayer.exe" /a "%1"`；ProgID 形式为 `Audio.<extension>`。

### 6.7 颜色按钮与 `ColorSelectCtrl`

- `0048CD60`、`0048CF13`、`0048CF91` 和公共选择器
  `004A0205`..`004A4944` 表明，颜色按钮第一次点击不是直接进入系统
  `ChooseColor`，而是创建类名为 `ColorSelectCtrl` 的 48 色弹层。
- 弹层使用原始样式 `0x94400100`、扩展样式 `0x100`，外框
  `156x171`；它锚定在按钮左下角，并限制在显示器工作区内。色块为 8 列、
  6 行，顶部是当前颜色/屏幕取色单元，底部文字取资源 `0x814B`
  “自定义...”。二进制色表中的第 46 项重复不是笔误，重建版保留了原始
  `COLORREF` 数据。
- 从顶部当前色单元开始拖动并在弹层外释放时，原版以 `GetPixel` 读取屏幕
  颜色；普通格直接回传颜色；只有底部自定义行才打开完整 `ChooseColor`。
- `FUN_004A471F` 创建、显示弹层后即返回，结果再由选择器通知送回页面。
  重建版因此使用无模态弹层和异步结果回调，不在 `BN_CLICKED` 中自建
  `GetMessage` 循环。`WM_CANCELMODE`、捕获被夺走、失活、Owner 销毁和再次
  打开另一色板均有有限收口，避免菜单/自动化调用栈长时间不返回。
- 同一选择器已覆盖视觉页六个颜色、播放列表/普通歌词/全屏歌词颜色，以及
  桌面歌词渐变编辑器的三个色标；结果才触发对应局部运行时掩码。

### 6.8 歌词搜索目录与网络子页

- 页 257 的目录对话框严格使用 `BIF_RETURNONLYFSDIRS`。标题取
  `0x8152`，动态 `BS_AUTOCHECKBOX` 取 `0x8153`；复选框样式为
  `0x50000003`、ID 为 0，并通过 `WM_COMMAND.lParam` 的 HWND 识别。
- “包括子目录”初始值对应进程内 `DAT_00547E88`。只有用户按 OK 且
  `SHGetPathFromIDListW` 成功时才更新；Cancel 和无效 PIDL 不改变它，也不
  写 XML。目录按 `_wcsicmp` 去重，有当前选中行时插入该位置，否则追加。
- 列表扩展样式是
  `LVS_EX_CHECKBOXES | LVS_EX_FULLROWSELECT | LVS_EX_LABELTIP`。配置路径只把
  首个 `*` 解释为递归标志；离开页面的
  `PSN_KILLACTIVE(-0xC9)` 才把当前顺序和勾选状态提交到设置对象。
- 网络缓存页 381 进入时强制打开并隐藏旧缓存开关，大小范围严格为
  `600..2000`；缓存目录和下载目录在非空且缺少尾反斜杠时补 `\`。下载并发
  数范围为 `1..5`，旧“覆盖文件”控件存在但隐藏。
- 原版页 258/381/382 后续会调用 `netacc.dll` 加速对象和下载管理器对象。
  当前仓库没有这些私有运行时对象，所以已恢复资源、设置、范围、目录格式与
  主窗口通知边界，但不能把纯 UI 状态描述成真实网络缓存或任务调度已恢复。

### 6.9 入口覆盖边界

- 当前可见主窗口、托盘回调、F1、皮肤按钮、视觉、播放列表、歌词和皮肤菜单
  都进入同一 15 页外壳，并保留各自的首选页。主入口 `0xE140` 先退出全屏并
  恢复 `Histroy/@LastActivePage`；定向入口不伪造主入口的全屏副作用。
- `0x7F4` 的内部“页 + 子模板/控件”路由也已保留：桌面歌词使用
  `(385,7)`，媒体库下载使用 `(382,9)`，页内链接则在现有属性表中切换，
  不从页面回调栈同步销毁再建窗口。
- 媒体库源模式和桌面歌词独立窗口现已接入运行时，并可分别发出 `0x7FEF`、
  `0x8046`；独立歌词搜索窗口及其 `0x0903` 来源仍未恢复。三条命令的主路由
  均保留。原版明确没有“选项”的播放列表空白/
  列名/工具栏菜单和全屏视觉/歌词菜单，重建版也不向这些位置添加额外入口。

## 7. 回归不变量与剩余边界

以下项目是后续改动必须保持的回归不变量；条目中明确指出的播放后端硬件和私有
运行时仍是剩余边界，不能用已经恢复的 UI 或探针结果替代：

1. Device 页必须保留四类设备发现、16 字节设备键、当前已恢复的能力字段和局部
   运行时通知；只显示 WAVE_MAPPER 或只保存 UI 字段不构成恢复。具体 waveOut
   设备键已经接到播放端；DirectSound、KS 和 ASIO 键均打开对应的原生后端，失败
   时不得兼容回退。ASIO 四行能力调用与格式已经恢复；由于当前宿主机没有 32 位
   ASIO 注册项，mock 只能证明调用顺序/格式，不能替代真实驱动验证。waveOut
   `dwSupport == 0` 的三行空白语义也是必须保持的已恢复不变量。
2. DSP 的勾选、顺序及真实 `Init/ModifySamples/Quit` 已接到 processor 链；
   评审时仍须确认 Options 页 259 的 Apply 会提升音频配置 revision，且
   `winamp_dsp_runtime_tests` 在隔离测试或宿主机回归中通过。
3. `FullScreen/@VisualType` 保存必须允许 0..3；全屏歌词与普通歌词的行间距都
   必须允许 0..100。普通歌词子页证据是 `FUN_00496A32` 对 spin `1044`
   调用 `FUN_0044ED1C(..., 0, 100)`，`FUN_0049734E` 再从 edit `1043`
   读取并以掩码 `0x10` 通知运行时。
4. Skin 默认 Author 必须来自内置皮肤元数据，预览右键导出和子控件合成不能
   遗漏。
5. HotKey 页激活/离开时必须严格注销/重建全局注册，避免编辑控件冲突。
6. 媒体库、网络、播放列表、歌词等页面的控件变化应发正确局部掩码；不要把
   每次 `EN_CHANGE` 扩大成同步重建全部音频/UI。
7. 任何从页内跳转到另一页的路径都应投递消息，不能在当前 DialogProc 栈中
   同步销毁整个属性表。

## 8. 动态验证证据

最初的原版发现矩阵和持久化探针在 Windows Sandbox 的
`WDAGUtilityAccount` 下运行，保留为历史基线：

- 页面顺序与入口：
  `rebuild/build/sandbox-settings/20260905-013813/`
- 关闭、Apply 与持久化：
  `rebuild/build/sandbox-settings/20260905-013331/`

按当前测试要求，最终验证直接在宿主机以独立临时产品副本运行，未启动
Windows Sandbox。探针仍沿用兼容字段名 `settings-sandbox-report.json`，该文件名
不代表此次执行环境：

- 预置色弹层无模态、无自建嵌套消息循环的 `ColorSelectCtrl` 专项：
  `rebuild/build/host-settings/20260905-170304-color-final-current/`。原版/重建版共 2 个场景，
  `Success/ExecutionSucceeded/BehaviorSucceeded/ParitySucceeded` 全为 true；
  无失败、断言、差异、错误或强制结束。该专项证明类名、样式、矩形、8x6 色格
  结构及“自定义...”路由，不做逐像素调色板比较，也不比较后续系统颜色对话框；
  预置色弹层是无模态的，而“自定义...”调用的 `ChooseColor` 仍是系统模态对话框。
- 最终 `Both / All` 双版本矩阵：
  `rebuild/build/host-settings/20260905-165607-all-final-current/`。原版与重建版各 20 个
  场景、各 195 次页面访问（合计 40/390）；四项成功标志全为 true，失败、
  断言失败、对比差异、错误和强制结束均为 0。报告不记录可执行文件哈希，因此
  它证明该目录所运行产品副本的结果，不单独构成任意后续二进制的身份凭据。

这 390 次访问覆盖 15 页以及 13 条探测入口：一个真实主窗口右键入口，加上 12 条
命令/私有消息路由。该次探针对媒体库、独立桌面歌词等入口直接注入对应消息，
因此这份历史结果只证明路由可接纳；现有媒体库源模式和桌面歌词窗口的运行时恢复
由各自实现与专项测试证明，不应倒推为这份矩阵已经覆盖。音频
设备页双方各 13 次访问均为 8 项、选中索引 4 且响应正常，但本机没有被接纳的
KS/ASIO 项，最终矩阵也未注入 helper 缺失、超时、崩溃或损坏快照；这些分支不在
本报告的证明范围内。

宿主机探针只读取或操作临时副本，并保持每项消息和进程退出有界。它不会确认
会产生外部副作用的网络请求、用户真实文件选择或实际文件关联修改；文件关联
另由 nonce 隔离的 HKCU 原生测试覆盖。
