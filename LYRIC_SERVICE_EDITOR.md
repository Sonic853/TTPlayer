# 歌词服务器编辑与 HTTPS

2026-09-14。在 5.7.9 歌词搜索恢复的基础上增加用户要求的列表编辑和 HTTPS，
这是有意扩展，不是声称旧 DLL 原本支持 HTTPS 或无限服务数量。

## 使用方式

- 每次打开选项窗口，后台读取一次**已加载歌词 DLL 同名、同目录的 `.ini`**。
  切换选项页不重读，也不在 UI 线程等待读取。
- “选项 → 歌词搜索”和手动在线搜索窗口的“代理设置”改为“编辑列表”按钮。
  代理仍在原“网络连接”页设置；此处不再跳转代理设置。
- 列表显示名称、地址、保存位置。DLL 内置条目显示对应 `.dll`，不可修改或删除；
  自定义条目显示对应 `.ini`，名称和地址可以编辑，保存位置始终只读。
  编辑列表及两个服务器下拉框均将所有 DLL 条目排在最前，INI 条目随后；
  DLL 保持固定顺序，INI 可以排序，刷新、保存和重新打开后保留排序及当前选中的服务器。
- 新增、删除、上移、下移竖排在列表右侧，复用“本地搜索”的原位图
  1027/1039/1042/1045、图像列表透明色、原生按钮输入与销毁清理。
  位图保留原始 16×15 尺寸；纯图标按 `0046E0C9` 的居中取整和按下 +1/+1 绘制，
  不再拉伸至 16×16，也不使用带文字按钮的左边距。详见 `OPTIONS_RECOVERY.md`。
  DLL 行不允许删除或移动；第一条 INI 不能上移至 DLL 区域，末行不能继续下移。
  未选中条目和保存期间禁用排序；命令处理和保存校验同样强制这些限制。
- 选中某插件的条目后点“新增”，新条目写入该 DLL 的同名 INI。
  多个插件分文件保存，未修改的文件不写；DLL 二进制不会改动。
- 编辑是草稿，点“保存”才异步写入并刷新下拉框，下一次搜索立即使用新地址。
  有未保存更改时关闭会询问；保存期间防止重复提交。
- 编辑窗口打开期间，禁用实际入口窗口（选项或“在线搜索并下载歌词”）的鼠标、键盘及
  关闭/应用/搜索/下载操作，再次进入只激活编辑器。即使两个入口都存在，也以点击的入口为准。
  保存完成不会提前启用入口窗口，关闭编辑器后才恢复；原本由其它窗口禁用的状态不会被错误恢复。
  播放器主窗口不被禁用，后台读取/保存仍使用原消息循环，不新增阻塞式等待。
  编辑器直接归属于被禁用的入口窗口，让 User32 处理点击该窗口时的模态激活/提醒闪烁，
  不手工调用 `FlashWindowEx` 或用定时器模拟动画。正常关闭前先恢复入口窗口的启用状态，
  再销毁编辑器，保证系统能将焦点还给入口；外部销毁仍有恢复兜底。
  未保存确认框出现时，重复入口或排队关闭命令会转向该确认框，不抢回编辑器焦点。
  从在线搜索进入编辑器时取消自动下载倒计时；后台请求继续，但其结果的 UI 应用/自动关闭
  延后到编辑器关闭以后，避免响应到达或曲目切换销毁未保存的编辑草稿。
- 编辑窗口采用与原选项页一致的字体和较小布局。当前宿主机实测 **436×369**，
  选项窗口 **558×458**，具体像素随 DPI 变化；位置居中于入口窗口所在屏幕。
  经由“主播放器 → 选项或在线搜索 → 编辑器”的 owner 链跟随普通/迷你模式各自的置顶选项。
  不设置独立、永久的 TOPMOST，也不在切换皮肤、模式时重建窗口。

例如 `AddIn/ttp_lrcsh.dll` 的自定义列表位于 `AddIn/ttp_lrcsh.ini`：

```xml
<ttp_lrcsvr>
  <server name="我的服务器" url="https://example.test/lyrics"/>
</ttp_lrcsvr>
```

虽然扩展名是 `.ini`，内容仍是原插件的 **UTF-8 XML**，不是 `[section]` 格式。
支持 UTF-8 BOM；保留合法 XML 的根属性、注释和 `<extra>` 等其它节点。
INI 自定义项与 DLL 内置项分别显示，不把内置项复制成可编辑的 INI 项。
主配置 `TTPlayerRebuild.xml` 只保存 `Lyric/AddInIndex` 和 `Lyric/ServerKey` 选择状态，
不是服务器地址清单。`ServerKey` 防止列表顺序变化或编辑名称后选错服务。
调整 INI 条目顺序后，`server` 节点增加非负 `order` 属性记录统一顺序。
因此不同 DLL 同名 INI 的条目也能相互排序，重读时不会按模块分组恢复旧顺序；
条目仍写入自己的原 INI，不改变保存位置。原插件忽略该额外 XML 属性。

保存会校验整个修改，再写同目录临时文件并原子替换，保留上一版本 `.ini.bak`。
打开后被外部修改、只读、不可访问、过大或损坏的 INI 会报告错误，不静默覆盖。
多文件保存若中途失败，已成功文件保留，失败文件的草稿保留供检查；不是跨文件事务。
每个 INI 限制 128 项、2 MiB；无已加载歌词插件时不能凭空新增虚假插件。

## 原版证据与兼容边界

`TTPlayer.exe.pseudo.c` 的 `00497AE5` 枚举歌词插件，`00497F63` 原本将 2185
转至网络选项；208/209 模板、自动搜索、候选选择和下载沿用既有恢复。
实际 `ttp_lrcsh.dll` 的反编译及本机 DLL 请求验证了：

| 地址 | 原版行为 / 当前处理 |
| --- | --- |
| `60351249` | 同名 INI 和 DLL STRINGTABLE；内置名称 32000/32001，地址 32002/32003 |
| `60351BE8` | XML `ttp_lrcsvr/server`，原 DLL 最多两个 server；新目录层支持多个自定义项 |
| `60351420` | 只识别 HTTP，旧 `svrlst` 可改写/删除 INI；新编辑列表不接受远端覆盖 |
| `6035249B`、`60352A6F`、`603529F6`、`60352BC0` | 查询文字清理、繁简/大小写映射、UTF-16LE 十六进制搜索参数 |
| `603531E7` | UTF-8 歌手+歌名、带符号字节和 32 位溢出的下载 Code 算法 |

对资源地址已知的千千协议服务，主程序在工作线程中发出
`?sh?Artist=...&Title=...&Flags=0&`、`?dl?Id=...&Code=...&`，解析结果 XML 和 UTF-8 LRC。
HTTP 和 HTTPS 都使用同一条路径，避免 HTTPS/第三个条目落回旧 DLL 的限制。
不额外发送旧 `ci` 设备标识、不进行旧 `svrlst` 远端列表覆盖。
其它无法识别资源地址的歌词插件仍保留原私有搜索 ABI，不假定它们支持千千协议。

HTTPS 保留系统证书验证，允许 TLS 1.2/1.3、禁止 HTTPS 重定向降级为 HTTP。
代理配置支持直连、系统代理、指定 HTTP 代理；超时和取消不阻塞 UI。
网络响应限制 2 MiB，XML 禁止 DTD/外部实体；错误状态、无效 XML、空下载不会当作成功。
相关传输契约见微软的 [WinHttpOpen](https://learn.microsoft.com/en-us/windows/win32/api/winhttp/nf-winhttp-winhttpopen)
和 [WinHTTP option flags](https://learn.microsoft.com/en-us/windows/win32/winhttp/option-flags)。

**地址必须提供兼容千千搜索/下载协议的服务**，普通歌词网页或其它 JSON API 不会自动兼容。
这些测试不证明历史外网歌词服务器仍然可用。

## 宿主机验证

- `lyric_search_tests`：真实 x86 DLL 的本机回环请求，与新搜索 URL、下载 Code 比对；
  包括中文、繁体、标点、编号及括号版本名。实际选项按钮、只读控件、增删改、连续保存、
  第三个服务搜索/下载、外部 INI 重新读取、不随页切换刷新、选择身份及非阻塞取消。
- `lyric_service_catalog_tests`：多 DLL 文件归属、三条以上服务、HTTPS/IPv6、XML 转义和 BOM、
  不修改 DLL、保留其它 XML、备份、外部修改冲突、只读文件、非法 URL/DTD、坏 INI、配置往返。
  另验证 DLL 排序拒绝、INI/DLL 边界、双向移动及跨 INI 文件排序的保存/重读。
- `window_topmost_tests`：真实编辑窗口置顶/取消置顶、普通/迷你、皮肤切换及三个全屏返回路径；
  同时验证编辑 HWND 不被销毁重建。
  选项在所有转换期间保持禁用，编辑窗口关闭后恢复；UI 测试覆盖四个图标按钮与本地搜索
  的布局/图像列表契约、清空选择、保存期间禁用、重复进入选项、排队关闭及强制销毁恢复。
  检查 `GW_OWNER` / `GW_ENABLEDPOPUP` 的原生模态关系，并实际打开未保存确认框，
  验证“否”保留草稿与禁用状态、“是”恢复选项、嵌套时重复入口保持确认框激活，且不写 INI。
  在线搜索入口另测选项同时存在时的归属、排队搜索/下载/关闭拦截、嵌套确认框、
  后台结果/切歌不丢草稿、关闭恢复及强制销毁清理；主播放器保持可操作。
  在线搜索及其编辑器在置顶切换和普通/迷你切换中保留 HWND、所属关系和禁用状态。
- 正向 HTTPS：测试客户端对 `https://example.com/` 的正常证书验证请求成功。
- 反向 HTTPS：`src/lyrics/test_lyric_https.ps1` 创建临时自签名证书，启动本机 TLS 服务，
  确认 WinHTTP 以 12175 拒绝。**未向用户/系统证书存储导入证书，未禁用证书验证**。

```powershell
cmake --build out/png-6120 --config Release --target lyric_search_tests lyric_service_catalog_tests window_topmost_tests
ctest --test-dir out/png-6120 -C Release -R '^(lyric_search|lyric_service_catalog|window_topmost)_tests$' --output-on-failure
# 可选手动 TLS 测试，需要 Python 和 OpenSSL：
./src/lyrics/test_lyric_https.ps1 -Program ./out/png-6120/Release/lyric_service_catalog_tests.exe -OpenSsl C:/path/to/openssl.exe
```

所有写入测试使用唯一临时目录和 DLL 副本；不修改播放器目录现有 DLL、INI 或用户主配置。
