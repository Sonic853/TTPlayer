# 歌词视觉同屏：专辑图片背景

## 使用

进入“歌词视觉同屏”后，在视觉区域右键选择“专辑图片”。全屏视觉区的左键
轮换也增加该模式：梦幻星空 → 频谱分析 → 示波显示 → 专辑图片。
如果主窗口原本正在显示封面，进入同屏时直接使用专辑图片背景。
单独“视觉全屏”仍保留原来的三种动态效果；普通主窗口封面的布局不变。

全屏歌词区域的右键菜单顶部也提供“梦幻星空”“频谱分析”“示波显示”“专辑图片”，
其后用分隔线保留原歌词命令。同屏下勾选当前实际显示的效果，选择后立即按该效果的
全屏布局配置切换。单独歌词全屏没有正在显示的视觉效果，因此不勾选；选择任意效果
会在当前屏幕转入“歌词视觉同屏”，不退出全屏、不重建主窗口，也不改变播放位置或暂停状态。
普通／迷你歌词菜单不增加这些选项。

“选项 → 全屏显示”新增：

- 同屏视觉效果列表中的“专辑图片”，可设置独立的重叠／并列关系与歌词占屏比例；
  勾选“对所有视觉效果使用统一设置”时，也和其它效果一样使用 All 配置。
- “备用图片”：通过现代 IFileOpenDialog 选择，可清除。支持 PNG、JPEG、BMP、
  GIF、TIFF、ICO 等 WIC 能解码的图片。
- “透明度”：0% 为图片不透明，100% 完全隐藏图片；与 PNG 自带的透明通道相乘，
  不改变歌词透明度、字体、滚动、卡拉 OK 或高亮设置。

**不新增背景色配置。** 图片与“歌词全屏显示”里的“颜色 → 背景”使用同一个
`Lyric/@BkgndColorFS`。背景优先级为：

1. 当前歌曲的专辑封面；
2. 没有可解码封面时，使用用户选择的备用图片；
3. 两者都没有、读取失败或无法解码时，不画图片，只显示上述背景色。

背景图片始终保持比例、居中 cover 裁剪，允许放大，不使用普通封面的
“完整放入且不放大”布局。在并列布局下铺满视觉区域，在重叠布局下铺满屏幕。
“全透明”指图片层透明，不是让整个全屏窗口透出桌面。

## 与原版代码的关系

这是按用户要求新增的社区功能，不宣称原版 5.7.9 已有专辑背景全屏。
本次不使用 TTPlayer6120 的非皮肤实现作为参考。

- `0049ECD0 / 0049F14C`：全屏选项资源 263 的控件初始化与统一／分效果配置。
  原有 2232 组合框的三项后追加第四项；新增控件使用该模板底部空余空间，
  原“歌词全屏显示”分组及背景颜色按钮 1158 不动。
- `0046228D`：保留同屏矩形计算、重叠／并列布局、歌词比例和窗口分离／还原机制；
  将布局数组由 All + 三种效果扩展为 All + 四种效果，不复制歌词设置。
- `00458020`：扩展同屏视觉区域的右键菜单，保留既有专辑命令 `0x8089`，
  在这里将名称改为“专辑图片”；普通封面菜单和单独视觉全屏菜单不变。
- 全屏歌词菜单在 `004427B1` 对应的原有删项完成后插入四个社区扩展项，避免改变
  原按位置删项的含义；前三项复用 `ttpres.dll` 的字符串列表 2232，第四项复用 EXE
  的“专辑图片”资源。继续以主窗口为 `TrackPopupMenu` 的命令所有者，沿用
  `0x8086..0x8089 -> SetVisualType`，不启用 owner-draw 或另开菜单子层。
  `WM_INITMENUPOPUP` 同时识别这些命令，但不会给单独歌词模式误勾选隐藏的视觉效果。
- `004AD83E / 00457B11 / 00457E2D`：继续使用当前 reader 的封面元数据及既有
  `ReadEmbeddedPicture` 回退解析，不引入 Shell 缩略图、联网找图或自动 Folder.jpg。
  解码仍然 WIC 优先、OleLoadPicture 备用。

`album_background.cpp` 集中实现 cover 源矩形裁剪及图片混合。
`VisualRuntime` 在持有同一把渲染锁时完成配置与当前封面的装载，避免中间
备用图片帧被工作线程画出；完整图片在离屏缓冲区合成后一次提交。
图像或设置未变时复用已经合成的画面，避免逐帧缩放以及透明度重复叠加。
暂停后的重设尺寸／模式也会重新提供当前封面，切换到无封面歌曲不会残留上一首。

## 持久化

新增全局属性位于 `TTPlayer.xml/FullScreen`，不写入皮肤 XML：

| 属性 | 默认值 | 含义 |
| --- | --- | --- |
| `AlbumFallbackImage` | 空 | 备用图片路径；手工填写相对路径时相对当前 EXE 目录 |
| `AlbumTransparency` | `0` | 图片透明度百分比，限定 0–100 |
| `PosRelationAlbum` | `1` | 专辑模式的同屏关系，默认重叠 |
| `LrcSizeAlbum` | `2` | 专辑模式的歌词比例，默认 20% 高度 |

原 `FullScreen/VisualType` 允许 0–4。它仍是选项页正在编辑的布局配置索引，
不是新建的“当前播放效果”开关：0 表示统一配置，非 0 时运行时按 `Visual/Type`
选择对应布局。旧的 All/Goom/Spectrum/BlurScope 属性保留其读写语义。
缺少新属性的旧配置使用上述默认值，不需要迁移或复制用户文件。

## 验证

Release Win32 编译；宿主机执行 `fullscreen_album_tests`，覆盖：

- 横／竖图 cover 居中裁剪、小图放大、0/50/100% 透明度、PBGRA 与全局 alpha 混合；
- OLE 备用渲染路径；真实 WIC 编解码的 PNG 和透明 PNG；
- 当前歌曲封面优先、读者封面为空／损坏时回退、切歌、暂停重设布局、停止；
- 重复绘制无累积混合、无封面时不出现普通模式的文件信息提示；
- 真实 HWND 渲染及真实 TrackPopupMenu 的同屏菜单名称、勾选和单独视觉全屏菜单；
- 全屏歌词右键菜单顶部四项的顺序／命令 ID／文字／勾选／分隔线、原歌词与显示屏命令；
  真实弹出菜单经过主窗口 `WM_INITMENUPOPUP`，分别验证歌词全屏和同屏下的四种选择状态。
  独立 `WM_COMMAND` 回归遍历四种效果，验证从歌词全屏转入同屏、同屏内切换，
  不改变显示器、还原信息、歌词拖拽开关、播放／暂停状态或音频进度。
- 真实 5.7.9 资源 263 上的四项效果列表、新控件边界、独立／统一配置与滑块命令；
- 中文及 `&` 路径 XML 往返、旧配置默认值、越界值限制、共享背景色。

连同桌面歌词菜单、皮肤配置、标题定位、菜单图标、PNG 皮肤、基础功能、桌面歌词、
皮肤重绑定，共九项回归。测试使用临时配置与合成图片／音频元数据，不播放用户音频，
不改动现有 `TTPlayer.xml`、DLL、皮肤或皮肤配置；本次没有进行新的原版整机视觉比对。
可传入第三个参数输出实际选项页截图进行布局检查。
