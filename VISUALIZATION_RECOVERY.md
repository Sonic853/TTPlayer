# 主窗口视觉效果恢复记录

本模块按 `TTPlayer.exe.pseudo.c`、原版进程的 HWND/菜单/像素探针，以及随附
`ttpcomm.dll`/sound add-in 的 x86 ABI 共同恢复。视觉区域是独立的
`VisualCtrl` 子窗口（控件 ID `0x7DDC`），不是主窗口背景上的装饰性绘制。

## 效果状态与刷新链

全局 `Visual/Type` 的原版取值如下：

| Type | 效果 | 命令 |
| ---: | --- | ---: |
| 0 | 不显示 | `0x8085` |
| 1 | 梦幻星空 | `0x8086` |
| 2 | 频谱分析 | `0x8087` |
| 3 | 示波显示 | `0x8088` |
| 4 | 专辑封面 | `0x8089` |

`CPlayerWnd_Run` 仅在声频引擎状态为 2（播放）时取得两组 512 点有符号
16 位 PCM。重建版从 `waveOut` 的实际播放游标拼接这 512 帧，而不是读取尚未
播放的队尾 buffer。暂停保持最后一帧；停止和打开失败清除动态画面及旧封面。
刷新由独立工作线程按 `FramesPerSec ? 1000 / FramesPerSec : 50 ms` 执行。
该线程与原 `CPlayerWnd::Run` 一样，在更新渲染器后直接 `GetDC`/绘制/`ReleaseDC`，
所以主线程停在右键菜单的模态消息循环时动画仍会继续；配置、清理、工作线程更新和
`WM_PAINT` 通过同一渲染器锁串行化。

## 三种动态效果

- 梦幻星空沿用 `ttpcomm.dll` 序号 90–93 的 x86 fastcall 对象 ABI：创建、
  调整尺寸、以左右声道处理一帧、释放。普通模式使用皮肤视觉矩形的实际大小。
- 频谱沿用序号 105 创建的 512 点分析器；虚表槽 1 读取持久的 1536-short PCM
  工作区并将 256 个幅值写入第三段。柱高使用原版指数 bin 表、对数缩放、相邻
  平滑、每帧下降 2 像素、峰值延迟/加速下降和 `SpectrumWide` 的三列画一列空。
- 示波按 `FUN_0045724D/00457452/004574F7` 使用有边框的 8 位 mask、原地四邻
  域模糊、`BlurSpeed` 衰减和固定 x 列竖段，再以 mask 混合 `BlurScopeColor`
  与皮肤背景。

频谱和示波均持有可复用的 32 位 DIB；每帧先恢复换肤/重配时缓存的视觉区域皮肤
背景切片，再写效果像素，避免将上一帧回抓为背景、紫色键色泄露以及逐像素 GDI
调用造成的卡顿。工作区只在取得完整 512 点时更新；暂停冻结，停止同步恢复底图，
并按原版保留频谱峰值和示波余辉供下一首继续衰减。

## 皮肤配置合并

视觉矩形来自当前皮肤 `Skin.xml` 的 `player_window/visual`。颜色、宽频谱、
模糊和提示字体由三层配置组成：

1. 启动时读取根目录 `TTPlayer.xml/Visual`，其中 `Type` 和 `FramesPerSec` 为
   全局字段；
2. 运行中换肤时，以皮肤包 `Visual.xml` 的存在字段覆盖当前值；
3. 再以 `Skin/<name>.skn.xml` 的视觉快照覆盖颜色/字体等字段，但侧车不读写
   全局 `Type`/`FramesPerSec`。

这是稀疏合并而非整块替换。实测从默认皮肤切换至 Let's Vista 后，频谱像素
使用侧车最终值 `#194d5c`；LX-iPlay 的包内 `Type=4` 则在换肤时选择封面模式。
视觉字体兼容普通 LOGFONT 文本以及 `base64:` 编码的 60 字节 LOGFONTA/
92 字节 LOGFONTW，损坏字段不会清空上一层有效字体。

## 专辑封面 ABI

原版通过 reader 的 `QueryInterface` 请求 IID
`{B5E770AF-DFB0-43E5-9B0C-3EE98E7B6248}`（插件 RTTI 名
`ISoundThumbnail`），见 `FUN_004AF074`。`FUN_004AD83E` 直接调用虚表槽 6：

```cpp
GetAt(0, const LegacyThumbnailEntry** entry);
```

只有返回值恰为 `S_OK` 才接受；播放器固定复制第 0 张，不按 picture type 搜索。
返回结构为 x86/pack(4) 的 24 字节记录，blob 由插件拥有，必须在释放接口前复制。
MIME 判定按 `FUN_004AD0AD` 区分大小写：直接接受 `image/jpeg`、`image/jpg`、
`image/bmp`、`image/gif`；空值、`image/`、`image/*` 才按 JPEG/BMP/GIF 魔数
嗅探。AAC、APE、ASF、FLAC 和 RM reader 因而共享原版封面来源。

接口存在但第 0 张为空/无效时不会跨过插件再私自扫描文件。不存在 reader 封面
接口且文件也没有内嵌图时，封面模式按 `FUN_00457C33/00457D90` 显示
`ttpres.dll` 字符串 `0x821B`。封面只会等比缩小并居中，不会放大。

按项目当前兼容要求，接纳后的原始图像先交给 WIC 解码，WIC 失败才回退到原版的
`OleLoadPicture`/`IPicture` 路径。这一优先级是明确的兼容扩展：参考 FLAC 的第 0
张记录把 PNG payload 标成 `image/jpeg`，原版 OLE-only 路径显示空白，而重建版
可由 WIC 正确显示该 PNG；reader ABI、第一张选择和 MIME 接纳规则仍保持原版。

## 鼠标与菜单

`FUN_00457EF5` 的视觉专属输入只有 `WM_LBUTTONUP`、`WM_CONTEXTMENU` 和
`WM_SETCURSOR`：

- 普通左键释放发送 `WM_COMMAND(0x7DDC)` 并按 `0→1→2→3→4→0` 轮换；
- 只有 Type 4、无封面且命中“添加专辑封面”文字时通知码为 1，并打开当前
  播放文件的属性；该矩形内才显示手形光标；
- 没有视觉专属的按下捕获、拖拽或双击动作，所以在视觉区按下后移出再释放
  不会误切换，也不会拖动主窗；
- 普通视觉区右键由资源 145 和 390 拼成原版 10 项菜单；全屏视觉区按
  `FUN_00458020` 裁成 7 项，且不套用普通皮肤化菜单框架。

右键菜单的“视觉效果选项”使用 `ttpres.dll` 对话框 253：类型、10–40 FPS、
频谱宽柱、峰/顶/中/底颜色、示波颜色/模糊/衰减、提示文字颜色和字体均即时回刷；
配置文件按钮沿用资源菜单 158，并读写 `.ttvi_cfg`（预设不包含 FPS）。

## 全屏状态机

命令 `0x7DE8/0x7DE9/0x7DEA` 分别对应歌词、视觉、歌词视觉同屏，状态值为
1/2/3；`0x7DE6` 在普通状态进入同屏，在全屏状态退出。普通状态仅播放中可
进入，进入后即使暂停或停止仍能切换和退出。

视觉全屏保持 `WS_CHILD`，添加 `WS_EX_TOOLWINDOW` 后用 `SetParent(nullptr)`
挂至桌面；歌词控件才执行 `WS_CHILD→WS_POPUP`。主窗同时隐藏并最小化，ESC
全局热键、`SC_RESTORE` 或显式退出会恢复父窗口、样式、矩形和主窗状态。
Type 0/4 进入视觉全屏时强制为 Type 1；全屏左键只在 1–3 间循环。

同屏布局读取 `TTPlayer.xml/FullScreen` 的 `VisualType`、四组
`PosRelation*`/`LrcSize*`。当前默认配置按效果分别取布局：Type 1 的
`PosRelationGoom=1` 使视觉覆盖 1920×1200、歌词占底部 1920×240；Type 2
的 `PosRelationSpectrum=0` 则产生视觉 1920×960、歌词 1920×240。全屏内部
画布按原版上限渲染后拉伸：梦幻星空 320×240、频谱 240×180、示波
512×384；全屏背景为黑色，不截取皮肤。

## 验证

- Debug Win32 构建成功；自动测试 6/6 通过。
- 全量扫描 173 个外部皮肤：145 个 `Visual.xml` 均完成稀疏解析；有正常/迷你
  `visual` 节点的皮肤按各自矩形创建，没有该节点的皮肤保持隐藏而不伪造区域。
- 默认皮肤、Let's Vista、TT2012、LX-iPlay 和最大 316×189 视觉区的
  `035 KK Player` 均完成运行验证；五种 Type 可连续切换且进程正常退出。
- 实际播放期间 Type 1/2/3 均产生连续变化帧；普通菜单为 10 项，全屏菜单
  为 7 项；左键轮换与移出释放行为和原版一致。
- 原版与重建版同配置动态探针确认：歌词/视觉为 1920×1200；同屏 Type 1
  为 1920×1200 覆盖布局，Type 2 为 1920×960 + 1920×240 并列布局；主窗
  `Visible=0/Iconic=1`，ESC 恢复。
- 参考 FLAC 的 reader ABI 返回 713393 字节、PNG 头 `89504E47`；WIC-first
  版本已验证 Type 4 显示封面。普通无封面 WAV 仍显示资源 `0x821B` 提示并保留
  原版文字命中/手形光标行为。
