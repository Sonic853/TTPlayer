# 桌面歌词按钮悬停闪烁

本次只以 5.7.9 `reverse/decompiled/TTPlayer.exe.pseudo.c` 为原程序依据。
不把 TTPlayer6120 的非皮肤代码作为参考，也不将新增的 PNG 合成方式宣称为原版二进制实现。

## 原版为何不会整条工具栏反复闪动

| 原版地址 | 从伪代码确认的行为 |
| --- | --- |
| `004186BC → 00452FFF → 0040A1DD` | 为各个独立的 SkinButton 子窗口安装四帧位图、透明区域和位置；并非由父窗每次重画所有按钮。 |
| `0040A5E3` | 检查鼠标是否在按钮内，处理捕获及普通／悬停／按下状态；新状态等于 `+0x34` 时直接返回，只有变化时才 `InvalidateRect(button, ..., TRUE)`。 |
| `0040A445 → 0040F288 → 00426434` | 按钮的 `WM_ERASEBKGND` 返回 `1`，不实际清底。这里的 `InvalidateRect(..., TRUE)` 不代表随后会用背景色擦掉图像。 |
| `0040FC78 → 0040A35B` | `BeginPaint` 后以位图宽度除以四定位当前帧；从选入内存 DC 的原图经 `BitBlt(SRCCOPY)` 或透明绘图分支输出该按钮，最后 `EndPaint`。这里的内存 DC 是源位图 DC，不能误称原版在双缓冲重画整条工具栏。 |
| `00416541 → 0041A7A5 → 0041AA08/0041AA1C → 0041AB1F` | 父工具栏的擦除／绘制走皮肤背景路径，与按钮状态更新分离。 |

原版的关键是不重复失效、不实际擦空按钮、只更新需要改变的控件。

## 重建版的差异与修复

原重建版把按钮绘制集中在 `DesktopLyricsWindow::Impl::PaintBar`，子 HWND 负责鼠标和提示。
每个 `WM_MOUSEMOVE` 都无条件使整个父工具栏失效，`PaintBar` 又直接向可见 DC 执行
“填透明色 → 背景 → 图标 → 逐个按钮”。这些中间画面在透明工具栏上可能表现为按钮闪烁，
在 PNG 图层逐个混合时尤其明显；仅拦截 `WM_ERASEBKGND` 不会阻止 `PaintBar` 自己清底。

现修复为：

- 相同悬停／按下状态不再失效；旧按钮迟到的离开消息不影响新按钮。
- 状态变化只失效相关子控件在父工具栏上的矩形；移动到其它按钮不重画无关区域。
- 背景、图标、BMP／PNG 当前帧在离屏 DC 中合成完毕，再一次 `BitBlt` 提交完整结果。
  `WM_PAINT` 的 DC 裁剪区限制实际更新范围；`WM_PRINTCLIENT` 使用同一合成路径。
- 按住后移出按钮显示正常帧，移回恢复按下帧；外部释放不执行命令。
  取消操作释放鼠标捕获，禁用／重新启用会更新对应帧。
- 保留 PNG 的 source-over 混合和旧 BMP 绘制结果，不通过禁用高亮、改变透明度或隐藏控件规避闪烁。

这不是逐字替换原版的私有窗体类；是保留现有 PNG 兼容结构，同时恢复上述失效／绘制原则。

## 验证

`desktop_lyrics_menu_tests` 在宿主机交互桌面串行执行：

- 100 次同按钮内移动无新增失效；单按钮变化的失效矩形等于其按钮矩形；
- 离开、迟到的离开、按下、移出／移回、取消、外部释放、禁用／启用状态；
- 100 轮 PNG 悬停／离开像素一致，透明边缘保留背景，不累积 alpha；
- 对目标 DC 记录增强图元文件，确认只有一次最终 `SRCCOPY`，没有可见清底或逐按钮绘制记录；
- 真实移动宿主机鼠标，遍历 PNG 测试工具栏的 4 个控件和 TT2012 BMP 工具栏的 12 个控件，
  统计 `WM_PAINT`：同控件内继续移动不再重复绘制，工具栏保持可见；
- 既有动态悬停提示、曲目菜单、预设及 A+/A- 字号测试继续执行。

测试只使用临时 PNG／配置及资源文件，模拟鼠标后恢复位置与前台窗口，不播放或修改用户音频。
这里没有新增对原版运行中画面的高帧率录像比较；原版结论来自上述伪代码调用链。

Release 构建通过；宿主机执行 `desktop_lyrics_menu_tests`、`desktop_lyrics_tests`、
`fullscreen_lyric_drag_tests`、`fullscreen_album_tests`、`progress_seek_tests`、
`ttplayer_tests`、`skin_profile_tests`、`skin_rebind_tests`，8 项全部通过。
